# amd64 + NVIDIA GPU 전용 서비스 (CUDA 필요: Whisper STT, SD.Next 이미지 생성)
# 사용법: docker compose -f docker-compose.yml -f docker-compose.amd64.yml up -d
# 또는:   scripts/deploy.sh (아키텍처 자동 감지)

services:
  # ============================================
  # Whisper (STT) — fedirz/faster-whisper-server (CTranslate2 기반, OpenAI Audio API 호환)
  # CTranslate2 사용으로 PyTorch CUDA 커널 의존도가 낮아 Blackwell 등 신형 GPU 호환성이 좋음.
  # latest-cuda 태그가 amd64 + NVIDIA GPU 전용이라 이 파일에 위치.
  # ============================================
  whisper:
    container_name: whisper
    image: fedirz/faster-whisper-server:latest-cuda
    restart: always
    environment:
      WHISPER__MODEL: Systran/faster-whisper-large-v3
      WHISPER__INFERENCE_DEVICE: auto
      UVICORN_PORT: "8000"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    networks:
      - kloudchat

  # ============================================
  # SD.Next (이미지 생성 — A1111 API 호환)
  # LibreChat SD_WEBUI_URL=http://sdnext:7860 으로 직접 연동 가능.
  # ============================================
  sdnext:
    container_name: sdnext
    image: vladmandic/sdnext-cuda:latest
    restart: always
    volumes:
      - ./sdnext/models:/mnt/models
      - ./sdnext/outputs:/mnt/outputs
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    networks:
      - kloudchat
